# Core Python Data Science Libraries (USED)
pandas==1.5.3
numpy==1.24.1
matplotlib==3.6.3
scikit-learn==1.2.1

# Deep Learning Frameworks (USED)
torch==1.13.1
torchaudio==0.13.1
torchvision==0.14.1
tensorflow==2.8.2  # Used in FLUENT_PLAYGROUND legacy notebooks

# Natural Language Processing (USED)
transformers==4.45.0
nltk==3.9.1
sentence-transformers==3.0.1

# Jupyter Notebook Support (USED)
jupyter>=1.0.0
ipykernel==6.9.1
notebook>=6.4.0
ipython==8.4.0
ipywidgets==8.0.4

# Data Processing and Utilities (USED)
openpyxl==3.1.5
PyYAML==6.0.2

# Model Training and Monitoring (USED)
neptune==1.10.4
torchinfo==1.8.0

# HTTP Requests and File Handling (USED)
requests==2.32.3
urllib3==1.26.19

# Scientific Computing (USED)
scipy==1.10.0

# Progress Bars and UI (USED)
tqdm==4.67.1

# Data Validation and Type Hints (USED)
typing-extensions==4.4.0

# File System Operations (USED)
fsspec==2024.6.1

# Tokenization Support (USED)
tokenizers==0.20.3

# Additional ML Tools (USED)
datasets==2.20.0
huggingface-hub==0.24.5

# Mathematical Operations (USED) 
sympy==1.13.3

# Memory and Performance (USED)
psutil==6.0.0

# Threading and Multiprocessing (USED)
threadpoolctl==3.1.0

# Development Tools (USED)
virtualenv==20.26.6

# Additional utilities that are needed (USED)
six==1.16.0
certifi==2022.6.15
charset-normalizer==2.0.4
idna==3.10
packaging==23.0
python-dateutil==2.8.2
pytz==2022.7.1

# Additional NLP and ML libraries from working environment (USED)
regex==2024.7.24
safetensors==0.4.4

# Additional utilities from working environment (USED)
joblib==1.2.0
filelock==3.15.4
click==8.1.7
Jinja2==3.1.6
MarkupSafe==2.1.2
protobuf==3.19.6
dill==0.3.8

# Additional torch utilities (USED)
torchmetrics==1.4.1
torchsummary==1.5.1

# Standard library modules (built-in, no need to install)
# json, math, time, os, collections

# REMOVED PACKAGES (NOT USED IN CODEBASE):
# pyaml==25.7.0                    # Not found in imports
# networkx==3.3                    # Not found in imports  
# zstandard==0.23.0               # Not found in imports
# gensim==4.3.3                   # Not found in imports
# opencv-python==4.10.0.84        # Not found in imports
# opencv-python-headless==4.10.0.84  # Not found in imports
# Pillow==9.4.0                   # Not found in imports
# beautifulsoup4==4.13.1          # Not found in imports
# plotly==5.24.1                  # Not found in imports
# seaborn==0.13.2                 # Not found in imports
# tensorboard==2.8.0              # Not found in imports
# tensorboard-data-server==0.6.1  # Not found in imports
